name: zip
version: 0.3.1
description: Package your file as .zip (no extra deps).
keywords:
  - package
properties:
    input:
      type: string
      required: true
    output:
      type: string
      required: true